option_settings:
  aws:autoscaling:launchconfiguration:
    DisableIMDSv1: true
  aws:ec2:instances:
    EnableSpot: false
  # This setting is often implicit but forcing a modern platform usually helps.
  # If the account blocks Launch Configurations, EB should attempt Launch Templates
  # if the platform supports it. 
